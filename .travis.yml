language: rust
rust:
    - stable
    - beta
    - nightly
matrix:
    allow_failures:
        - rust: nightly
    fast_finish: true
install:
    ./ncurses-install-compile
script:
    - cargo build --verbose
